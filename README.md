# Spotify-Analytics with dbt

This project extracts data from Spotify Web API data, loads it into a data warehouse (Snowflake), and uses dbt for data modeling.

# Tech Stack
- Python ('spotipy', 'pandas')
- dbt
- Snowflake
